# vanilla_catalog
A dictionary about MC setting for each version.

This repository also stores pinned dependencies used by the deployer.

## deps/plugins

- `invsee/InvSeePlusPlus.jar`
- `openinv/OpenInv.jar`
